  <view class="tabs">
    <view class="tabs_title">
      <view  url="" wx:for="{{tabsList}}" wx:key="id" class="title_item {{item.isActive?'active':''}}" bindtap='handleItemChange' data-index='{{index}}'>{{item.value}}
      </view>
    </view>
    <view class="tabs_content">
      <slot></slot>
    </view>
  </view>
