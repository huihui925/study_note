# 1 优化代码

+ 学会优化代码:
     - 封装工具函数
     - 封装发送接口请求的代码
     - 日常代码,重复代码段,统一写

# 2 捕获错误

当一些情况会报错,但是不影响项目正常使用时,我们可以通过try catch捕获错误,try里面写正常代码,和平时一样,若有错误就会被catch到,这样就不会报错了

```js
		try {
      //这里面写正常的业务代码
    } catch (error) {
      //对错误的处理 这里进行打印操作
      console.log(error)
    }

```

